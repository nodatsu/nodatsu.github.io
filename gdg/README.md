# 参加者向けページ

プレ道場(2026年8月9日)当日、参加者に開いてもらうページです。
`index.html` 1枚で完結しています。外部ライブラリは使っていません。

## GitHub Pages への置き方

1. 新しいリポジトリを作る(公開)
2. このフォルダの `index.html` と `dojo_loop.mp3` を置く
3. Settings → Pages でブランチを指定して公開
4. 表示されたURLの短縮URLとQRコードを作り、当日に掲示する

> **`完成例_道場主用/` は絶対に同じリポジトリに入れないでください。**
> リンクを張らなくても、公開された時点でURLは辿れます。
> 参加者が完成形を見ると、全員の作品がその劣化コピーになります。

置くのは次の2つだけです。

```
リポジトリ/
├── index.html      ← このフォルダのもの
└── dojo_loop.mp3   ← 音源
```

## 公開前にやること

`index.html` の末尾近くに、編集する場所が1か所だけあります。
URLを入れると、対応するボタンがページに現れます。**空のままだとボタンは隠れます。**

```js
const SETTINGS = {
  提出先URL:      '',   // 作品のコードを貼ってもらう共有ドキュメント等
  コミュニティURL: '',   // GDGコミュニティページ / 登録フォーム
  アンケートURL:   ''    // 当日アンケート
};
```

3つとも空でもページは成立します。決まったものから順に入れてください。

### 音源のURLは自動で入ります

参加者がコピーする `sketch.js` の中の音源URLは、**ページ自身の場所から自動で組み立てます。**

```js
const OTO_URL = 'https://<ユーザー名>.github.io/<リポジトリ名>/dojo_loop.mp3';
```

`dojo_loop.mp3` を `index.html` と同じ場所に置いておけば、書き換えは不要です。
GitHub Pages は CORS を許可しているため、p5.js Web Editor から直接読み込めます。
**参加者は音源をアップロードする必要がありません。**

ローカルでファイルを直接開いた場合(`file://`)は、相対パス `dojo_loop.mp3` のままになります。

## ページの構成

| 節 | 内容 |
|---|---|
| 1 準備 | sketch.js / style.css / index.html をコピーボタンで配布。貼る順番を明示 |
| 2 動かす | ▶ を押す → 画面をクリック → 手を挙げる |
| 3 三つの鉄則 | ひとつずつ頼む / 動いたら保存 / エラーはAIに貼る |
| 4 AIを開く | Gemini・Copilot・duck.ai。ゲストで使えることと、上限時の乗り換えを明記 |
| 5 お題 | 基本5つ・発展4つ・「説明してください」 |
| 6 ドームのコツ | 暗く / 太く大きくゆっくり / 中心を主役に |
| 7 困ったとき | 症状別の対処。最後は「手を挙げてください」 |
| 8 作品を出す | コードをコピーして提出 |
| 9 今日のあとに | 9月からの道場案内、アンケート |

## 設計の意図

- **暗い配色にしてあります。** ドームの部屋は暗いため、白い画面はまぶしく、
  スクリーンに余計な光が乗ります。
- **コピーボタンは3つのファイルすべてにあります。** 一部だけ書き足す方式はミスを誘発するため、
  全文の上書きに統一しています。
- **コードは折りたたんであります。** 「コードを見る」を押すと展開します。
  ページを短く保ちつつ、中身も確認できます。
- **クリップボードが使えない環境では、自動で選択状態にします。**
  ボタンの表示が「Ctrl+C でコピー」に変わります。

## 当日までに確認すること

- [ ] 実機のブラウザでページを開き、3つのコピーボタンが動くか
- [ ] コピーした3つを p5.js Web Editor に貼って、実際に音が鳴るか
- [ ] スマートフォンで開いたときに読めるか(高校生はスマホで見る可能性がある)
- [ ] 短縮URLとQRコードを作り、掲示物を用意したか

**Wi-Fi情報はこのページに載せないでください。**公開ページです。会場の掲示物にとどめてください。
